#include <stdio.h>

int main(void)
{
  double x, y, ans;
  
  x = 2.0, y=5.0;
  printf("x = %f \n", x);
  printf("y = %f \n", y);
  ans = x*y;
  printf("x*y = %f \n", ans);

  return 0;
}
